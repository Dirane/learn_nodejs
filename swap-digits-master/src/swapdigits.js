const swapDigits = (number)=>{
    let swappedNumber = 0
    //write logic here
    if(number < 0 || number.length == 0){
        swappedNumber = 0;
    }
    return swappedNumber;
    
}

module.exports = swapDigits
